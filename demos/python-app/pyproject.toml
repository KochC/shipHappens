[project]
name = "shiphappens-python-demo"
version = "0.1.0"
description = "Tiny app used to demo a Ship Happens CI pipeline"
requires-python = ">=3.10"

[tool.ruff]
line-length = 100

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
